# CODEOWNERS — required reviewers per path.
#
# NOTE: Currently scoped to @glt8810 (solo personal repo). When the repo moves
# to an organization with a team, replace each owner with the appropriate team
# handle, e.g. @org/security-reviewers. The path layout below already reflects
# the intended separation of concerns.

# Crypto code — mandatory security review. Never merge without it.
/packages/encryption/          @glt8810
/docs/adr/0002-crypto-design.md @glt8810

# Database schema — needs backend-lead review to catch migration footguns.
/packages/db/                  @glt8810

# CI and security workflows — infra owners only.
/.github/workflows/            @glt8810

# Default — everything else requires at least one code review.
*                              @glt8810
